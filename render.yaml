services:
  - type: web
    name: stock-tracker-bot
    runtime: python
    buildCommand: pip install -r requirements.txt && python -m pip show gunicorn || true
    startCommand: python -m gunicorn app:app
